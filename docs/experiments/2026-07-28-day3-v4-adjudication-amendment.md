# Day 3 BR-10 v4 adjudication amendment

Three static adjudicator versions exposed successive assumptions about variable names,
inline layout and helper shape. Kimi R2 direct then used a new but equivalent
`repo_is_ahead` / `PUSH_IF_AHEAD` decomposition, demonstrating that adding more regular
expressions would continue to overfit implementation form.

V4 executes the shell implementation against isolated temporary Git repositories and bare
local remotes. It verifies an already-committed ahead change is pushed with a clean working
tree, an oversized staged stock file is neither committed nor pushed, and the independent
incremental ahead commit still reaches its remote. Windows batch behavior remains a
structural slice because cmd.exe is unavailable on the experiment host.

V1-v3 scores remain frozen. The partially generated BR-11 DeepSeek R1 plan was
operator-interrupted during diagnosis after 334,058 model tokens and 22 tool calls. Its raw
evidence is excluded, and a new suffixed attempt replaces it.
